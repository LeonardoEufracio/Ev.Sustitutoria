import {Component} from '@angular/core'; import {FormsModule,NgForm} from '@angular/forms'; import {Router} from '@angular/router'; import {Api} from '../core/api.service';
@Component({imports:[FormsModule],template:`<main class="auth-page"><section class="auth-hero"><div class="hero-copy"><div class="logo-mark">GU</div><p class="eyebrow">Plataforma empresarial</p><h1>Movilidad organizada.<br><span>Operación bajo control.</span></h1><p>Administra solicitudes, flota, conductores y rutas desde un único espacio seguro.</p><div class="hero-features"><span>✓ Control en tiempo real</span><span>✓ Trazabilidad completa</span><span>✓ Seguridad por roles</span></div></div></section><section class="auth-panel"><form #form="ngForm" (ngSubmit)="submit(form)" novalidate><div class="mobile-brand"><b>GU</b> Gestión de Unidades</div><p class="eyebrow">Bienvenido</p><h2>Iniciar sesión</h2><p class="muted">Ingresa con tus credenciales institucionales</p>@if(error){<div class="alert alert-error">⚠ {{error}}</div>}<label>Correo institucional<div class="input-wrap"><span>✉</span><input type="email" name="email" [(ngModel)]="email" #mail="ngModel" autocomplete="username" placeholder="usuario@empresa.com" required email></div>@if(mail.invalid&&mail.touched){<small class="field-error">Ingresa un correo válido.</small>}</label><label>Contraseña<div class="input-wrap"><span>⌑</span><input [type]="showPassword?'text':'password'" name="password" [(ngModel)]="password" #pass="ngModel" autocomplete="current-password" placeholder="••••••••••••" required minlength="8"><button type="button" class="icon-button" (click)="showPassword=!showPassword">{{showPassword?'Ocultar':'Ver'}}</button></div>@if(pass.invalid&&pass.touched){<small class="field-error">La contraseña es obligatoria.</small>}</label><button class="btn btn-primary btn-lg" [disabled]="loading">{{loading?'Validando…':'Ingresar al sistema'}} <span>→</span></button><p class="secure-note">🔒 Conexión protegida mediante autenticación JWT</p></form></section></main>`})
export class Login {email='';password='';error='';loading=false;showPassword=false;constructor(private api:Api,private router:Router){}submit(form:NgForm){if(form.invalid){form.control.markAllAsTouched();return}this.loading=true;this.error='';this.api.login({email:this.email,password:this.password}).subscribe({next:()=>{this.password='';this.router.navigate(['/dashboard'])},error:e=>{this.loading=false;this.password='';this.error=e.error?.message||'No fue posible iniciar sesión.'}})}}
