import {Component} from '@angular/core'; import {Router,RouterLink,RouterLinkActive,RouterOutlet} from '@angular/router'; import {Api} from '../core/api.service';
@Component({imports:[RouterLink,RouterLinkActive,RouterOutlet],template:`<div class="app-shell" [class.menu-open]="menuOpen"><aside class="sidebar"><div class="brand"><span>GU</span><div><b>Gestión de Unidades</b><small>Movilidad empresarial</small></div></div><div class="profile"><div class="avatar">{{initials}}</div><div><b>{{user?.name}}</b><small>{{roleLabel}}</small></div></div><nav><small class="nav-label">MENÚ PRINCIPAL</small><a routerLink="/dashboard" routerLinkActive="active"><i>▦</i> Dashboard</a>@if(role==='SOLICITANTE'){<a routerLink="/solicitudes" routerLinkActive="active"><i>◫</i> Mis solicitudes</a>}@if(role==='COORDINADOR'||role==='ADMINISTRADOR'){<a routerLink="/bandeja" routerLinkActive="active"><i>☷</i> Bandeja de solicitudes</a><a routerLink="/programacion" routerLinkActive="active"><i>⌖</i> Programación</a><a routerLink="/vehiculos" routerLinkActive="active"><i>▣</i> Vehículos</a><a routerLink="/conductores" routerLinkActive="active"><i>♙</i> Conductores</a>}@if(role==='CONDUCTOR'){<a routerLink="/rutas" routerLinkActive="active"><i>⌁</i> Mis rutas</a>}@if(role==='ADMINISTRADOR'){<small class="nav-label section">ADMINISTRACIÓN</small><a routerLink="/auditoria" routerLinkActive="active"><i>◎</i> Auditoría</a>}</nav><button class="sidebar-logout" (click)="logout()"><i>↪</i> Cerrar sesión</button></aside><section class="workspace"><header class="topbar"><button class="menu-toggle" (click)="menuOpen=!menuOpen">☰</button><div><b>Centro de control de movilidad</b><small>Operación y seguimiento de flota</small></div><div class="top-actions"><span class="system-online"><i></i> Sistema operativo</span><div class="mini-avatar">{{initials}}</div></div></header><main class="content"><router-outlet/></main><footer>Gestión de Unidades · Plataforma empresarial</footer></section>@if(menuOpen){<div class="scrim" (click)="menuOpen=false"></div>}</div>`})
export class Shell {user:any;role='';menuOpen=false;constructor(private api:Api,private router:Router){this.user=api.user();this.role=this.user?.role?.name||''}get initials(){return (this.user?.name||'U').split(' ').slice(0,2).map((x:string)=>x[0]).join('').toUpperCase()}get roleLabel(){return ({ADMINISTRADOR:'Administrador del sistema',COORDINADOR:'Coordinador de transporte',CONDUCTOR:'Conductor',SOLICITANTE:'Solicitante'} as any)[this.role]||this.role}logout(){this.api.logout();this.router.navigate(['/login'])}}
